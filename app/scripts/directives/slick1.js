(function(angular, $){
	// Needs:
	// Set up all available options
	// we want slides to show and speed to be dynamic
	// We want to listen to the after change

	angular.module('tdSlick', [])
}(this.angular, this.jQuery))