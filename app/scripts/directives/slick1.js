(function(angular, $){
	/*
	<td-slick slick-options="{arrows: true, dots: true}" 
		slides-to-show="slidesToShow" 
		speed="speed" 
		after-change="afterPage()">
		
		<div data-ng-repeat="item in [1,2,3,4] track by $index">{{item}}</div>
	</td-slick>
	*/
	angular.module('tdSlick', [])
}(this.angular, this.jQuery))
